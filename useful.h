#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

//reads a string rappresenting a number and transforms it into an int
void stoi(char* string, int * n);

//generate the string rappresentig a number (n<100)
void itos(int n, char*string);